//write a function that polls a given URL every X seconds
// and returns an observable with the polled data.

// run the function to poll a chuck norris fact every 2 seconds
// and console.logs it.


//usage:
// poll('https://api.itunes.com/songs', 3).subscribe(results => {...})
